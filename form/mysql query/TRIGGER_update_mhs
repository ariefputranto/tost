DELIMITER |
CREATE TRIGGER updateMHS
AFTER INSERT ON `BUILD_FORMULIR_SURAT_KETERANGAN_MAHASISWA_1460283183_CORE` FOR EACH ROW
BEGIN
	UPDATE tableMHS SET noHp = NEW.NOHP, email = NEW.EMAIL, noHpOrtu = NEW.NOMOR_HP_ORTU WHERE nrp = NEW.NRP;
END;
|
DELIMITER ;
